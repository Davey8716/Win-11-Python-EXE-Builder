**bundle dlls with it  so it can run outside the folder its made in?**



**make the build log file be put inside the build folder unless built to desktop?**



**add install pip install logic to the build so you know you need modules installed on each interpreter before building**



